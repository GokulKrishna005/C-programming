#include<stdio.h>

int s,a,b;
int sum()
int main()

{
    // Funtion without arguement with return type
    R=sum();
    int sum();   // Here we have specified int so that the function returns int
    {
        printf("Enter a,b \n");
        scanf("%d %d",&a,&b);
        s=a*b;
        return s;
    }
    
    // With arguement without return type
    // Arguement is an actual parameter


}